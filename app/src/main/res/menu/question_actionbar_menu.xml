<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.toolbar.QustionFragment" >

    <item android:id="@+id/group_item"
        android:title="@string/toolbar_group"
        android:icon="@drawable/menu_refresh"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/search_item"
        android:title="@string/toolbar_search"
        android:icon="@drawable/menu_search"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/news_item"
        android:title="@string/toolbar_news"
        android:icon="@drawable/menu_settings"
        app:showAsAction="ifRoom"/>

</menu>